{
  "title": "Fib",
  "files": [
    {
      "name": "Fib.py",
      "content": "i = 0\n\ndef fib(n):\n  # din kod in här\n    if n < 2:\n        return n\n    else:\n        return fib(n-1) + fib(n-2)\n  \nprint(\"Fib: \" + str(fib(4)))\nprint(\"Antal anrop: \" + str(i))\n"
    }
  ]
}